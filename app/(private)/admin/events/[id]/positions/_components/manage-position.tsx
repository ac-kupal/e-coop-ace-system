import React from "react";
import PositionTable from "./position-table";

type Props = {
   id: number | undefined;
};

const ManagePosition = ({ id }: Props) => {
   


   return (
      <div>
      </div>
   );
};

export default ManagePosition;
